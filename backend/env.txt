# ================================
# LOOP HABITS BACKEND CONFIGURATION
# ================================
# Copy this file to .env and fill in your values

# MongoDB Connection String
# Get this from MongoDB Atlas: https://cloud.mongodb.com
# 1. Create a free account
# 2. Create a cluster
# 3. Click "Connect" > "Connect your application"
# 4. Copy the connection string
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/loophabits?retryWrites=true&w=majority

# JWT Secret Key
# Generate a random string (at least 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-make-it-long

# Server Port
PORT=5000

# Frontend URL (for CORS - update when deploying)
FRONTEND_URL=http://localhost:3000
